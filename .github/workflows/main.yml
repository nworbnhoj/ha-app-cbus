name: HACS Validation

on:
  push:
    branches:
      - master
      - main
  pull_request:
  workflow_dispatch:

jobs:
  hacs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: HACS validation
        uses: hacs/action@22.5.0